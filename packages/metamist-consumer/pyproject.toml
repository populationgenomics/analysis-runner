# Deployed as a Cloud Function with --runtime python311.
[project]
name = "metamist-consumer"
version = "0"
description = "Cloud Function that stores analysis-runner submission metadata in metamist"
requires-python = ">=3.11"
dependencies = [
    "google-cloud-logging",
    "metamist>=7.14.0",
    "requests",
]

[tool.uv]
package = false
